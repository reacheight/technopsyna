*Список команд:*

/bl — ыкнуть
/wf `<запрос>` (или /wolfram `<запрос>`) — запрос в wolframalpha (взято у [бота чата мехмата](https://t.me/algebrach_bot))
/voice `<текст>` (или /tts `<текст>`) — text to speech, отправляет голосовое сообщение
/deer\_message — случайное сообщение рядового оленя
/random\_test `math` или `inf` или `rus` или `phys` — получить случайный вариант ЕГЭ по соответствующему предмету.

Так же команды /wf и /voice поддерживают *Inline mode*!

/about или /start — небольшое описание бота
/help — список доступных команд
